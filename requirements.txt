# Dependencies for the project
# Requires Python 3.10+ (uses union type syntax: str | None)

torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# Optional: TTS support (requires piper-tts model download separately)
# piper-tts>=1.2.0
